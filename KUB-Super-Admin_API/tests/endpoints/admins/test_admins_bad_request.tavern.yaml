test_name: test admins endpoint for bad request

stages:
  - name: check admins post for bad request
    request:
      url: "https://stage.superadmin.api.kub.education/admins"
      method: POST
      headers:
        X-SUPER-ADMIN-KEY: "{tavern.env_vars.TOKEN}"
        Content-Type: "application/json"
        json:
          []
    response:
      status_code: 400